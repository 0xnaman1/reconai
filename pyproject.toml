[project]
name = "recon-ai"
version = "0.1.0"
description = "Chat-first LLM reconciliation assistant for bank and ledger statements."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
  "apps/api",
  "apps/worker",
  "packages/core"
]
